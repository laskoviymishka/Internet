<ul class="nav pull-right">
    @if (Request.IsAuthenticated)
    {
        <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">
            @User.Identity.Name
            <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>
                    @Html.ActionLink("Сменить пароль", "ChangePassword", "Account")
                </li>
                <li>
                    @Html.ActionLink("Выход", "LogOff", "Account")
                </li>
            </ul>
        </li>
    }
    else
    {
        <li class="dropdown"><a href="#" class="dropdown-toggle" data-toggle="dropdown">Guest
            <b class="caret"></b></a>
            <ul class="dropdown-menu">
                <li>
                    @Html.ActionLink("Вход", "LogOn", "Account")
                </li>
                <li>
                    @Html.ActionLink("Регистрация", "Register", "Account")
                </li>
                <li>
                    @Html.ActionLink("Восстановить пароль", "ForgotPass", "Account")
                </li>
            </ul>
        </li>
    }
</ul>
